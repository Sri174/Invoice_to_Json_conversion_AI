version: '3.8'

services:
  invoice-processor:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
    environment:
      - API_HOST=0.0.0.0
      - API_PORT=8000
      - DEBUG=true
      - OCR_LANGUAGES=eng,ara
    restart: unless-stopped
